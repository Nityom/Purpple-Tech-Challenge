[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=app --cov=pipeline --cov-report=term-missing --cov-fail-under=70"

[tool.coverage.run]
source = ["app", "pipeline"]
omit = ["tests/*", "dashboard/*"]

[tool.coverage.report]
show_missing = true
skip_covered = false
